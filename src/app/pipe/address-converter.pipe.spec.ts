import { AddressConverterPipe } from './address-converter.pipe';

describe('AddressConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new AddressConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
