import { HoverBorderDirective } from './hover-border.directive';

describe('HoverBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverBorderDirective();
    expect(directive).toBeTruthy();
  });
});
